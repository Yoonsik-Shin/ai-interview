// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.0
//   protoc               v6.33.4
// source: document.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { handleUnaryCall, UntypedServiceImplementation } from "@grpc/grpc-js";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";

export const protobufPackage = "document";

/** 임베딩 생성 요청 */
export interface GenerateEmbeddingRequest {
  /** 임베딩할 텍스트 */
  text: string;
}

/** 임베딩 생성 응답 */
export interface GenerateEmbeddingResponse {
  /** 임베딩 벡터 (384차원) */
  embedding: number[];
  /** 벡터 차원 수 */
  dimension: number;
}

export const DOCUMENT_PACKAGE_NAME = "document";

function createBaseGenerateEmbeddingRequest(): GenerateEmbeddingRequest {
  return { text: "" };
}

export const GenerateEmbeddingRequest: MessageFns<GenerateEmbeddingRequest> = {
  encode(message: GenerateEmbeddingRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateEmbeddingRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateEmbeddingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseGenerateEmbeddingResponse(): GenerateEmbeddingResponse {
  return { embedding: [], dimension: 0 };
}

export const GenerateEmbeddingResponse: MessageFns<GenerateEmbeddingResponse> = {
  encode(message: GenerateEmbeddingResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.embedding) {
      writer.float(v);
    }
    writer.join();
    if (message.dimension !== 0) {
      writer.uint32(16).int32(message.dimension);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateEmbeddingResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateEmbeddingResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 13) {
            message.embedding.push(reader.float());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.embedding.push(reader.float());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.dimension = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

/** Document 서비스 */

export interface DocumentServiceClient {
  /** 텍스트를 임베딩 벡터로 변환 */

  generateEmbedding(request: GenerateEmbeddingRequest): Observable<GenerateEmbeddingResponse>;
}

/** Document 서비스 */

export interface DocumentServiceController {
  /** 텍스트를 임베딩 벡터로 변환 */

  generateEmbedding(
    request: GenerateEmbeddingRequest,
  ): Promise<GenerateEmbeddingResponse> | Observable<GenerateEmbeddingResponse> | GenerateEmbeddingResponse;
}

export function DocumentServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = ["generateEmbedding"];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("DocumentService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("DocumentService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const DOCUMENT_SERVICE_NAME = "DocumentService";

/** Document 서비스 */
export type DocumentServiceService = typeof DocumentServiceService;
export const DocumentServiceService = {
  /** 텍스트를 임베딩 벡터로 변환 */
  generateEmbedding: {
    path: "/document.DocumentService/GenerateEmbedding",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: GenerateEmbeddingRequest): Buffer =>
      Buffer.from(GenerateEmbeddingRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GenerateEmbeddingRequest => GenerateEmbeddingRequest.decode(value),
    responseSerialize: (value: GenerateEmbeddingResponse): Buffer =>
      Buffer.from(GenerateEmbeddingResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GenerateEmbeddingResponse => GenerateEmbeddingResponse.decode(value),
  },
} as const;

export interface DocumentServiceServer extends UntypedServiceImplementation {
  /** 텍스트를 임베딩 벡터로 변환 */
  generateEmbedding: handleUnaryCall<GenerateEmbeddingRequest, GenerateEmbeddingResponse>;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
