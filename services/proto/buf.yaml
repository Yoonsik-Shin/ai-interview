version: v2

modules:
  - path: .
    name: buf.build/unbrdn-org/unbrdn-repo

lint:
  use:
    - STANDARD
  except:
    # 기존 필드 네이밍 호환성 유지 (필요 시)
    - FIELD_LOWER_SNAKE_CASE
    - SERVICE_SUFFIX
    - ENUM_VALUE_PREFIX
breaking:
  use:
    - FILE
