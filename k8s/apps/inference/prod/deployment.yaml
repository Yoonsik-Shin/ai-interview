apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm
  namespace: unbrdn
  labels:
    app: llm
    environment: prod
spec:
  replicas: 2
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: llm
  template:
    metadata:
      labels:
        app: llm
        environment: prod
    spec:
      # 고가용성(HA): Inference를 최대한 넓게 분산 배치
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: llm
      # Main 노드에만 배치 (안정성 확보 - Langchain/RAG)
      # 참고: STT/TTS/벡터화는 향후 별도 서비스로 분리하여 Preemptible 노드에서 실행
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: node-pool
                    operator: In
                    values:
                      - main
      imagePullSecrets:
        - name: ocir-secret
      containers:
        - name: llm
          image: ${IMAGE_REGISTRY}/unbrdn-krn-ocir-llm:${IMAGE_TAG}
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          envFrom:
            - secretRef:
                name: llm-secrets
            - configMapRef:
                name: llm-config
          resources:
            requests:
              memory: "1Gi"
              cpu: "200m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
      restartPolicy: Always
