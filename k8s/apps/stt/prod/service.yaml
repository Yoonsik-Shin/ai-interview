apiVersion: v1
kind: Service
metadata:
  name: stt
  namespace: unbrdn
  labels:
    app: stt
spec:
  selector:
    app: stt
  ports:
    - name: grpc
      port: 50052
      targetPort: 50052
      protocol: TCP
  type: ClusterIP
