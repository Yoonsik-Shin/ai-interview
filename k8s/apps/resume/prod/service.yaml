apiVersion: v1
kind: Service
metadata:
  name: resume
  namespace: unbrdn
  labels:
    app: resume
spec:
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      protocol: TCP
    - name: grpc
      port: 9090
      targetPort: 9090
      protocol: TCP
  selector:
    app: resume

