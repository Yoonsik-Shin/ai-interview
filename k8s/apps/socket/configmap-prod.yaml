apiVersion: v1
kind: ConfigMap
metadata:
  name: socket-config
  namespace: unbrdn
  labels:
    app: socket
    environment: prod
data:
  PORT: "4000"
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"
  KAFKA_BROKER: "kafka-cluster-kafka-bootstrap.kafka:9092"
  PYTHON_WORKER_URL: "http://inference:8000"
