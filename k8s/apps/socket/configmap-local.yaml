apiVersion: v1
kind: ConfigMap
metadata:
  name: socket-config
  namespace: unbrdn
  labels:
    app: socket
    environment: local
data:
  PORT: "4000"
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"
  KAFKA_BROKER: "kafka:29092"
  PYTHON_WORKER_URL: "http://inference:8000"

