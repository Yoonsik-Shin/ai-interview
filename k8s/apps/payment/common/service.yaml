apiVersion: v1
kind: Service
metadata:
  name: payment
  namespace: unbrdn
  labels:
    app: payment
spec:
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      protocol: TCP
    - name: grpc
      port: 9090
      targetPort: 9090
      protocol: TCP
  selector:
    app: payment

