apiVersion: v1
kind: Service
metadata:
  name: kafka-ui
  namespace: unbrdn
  labels:
    app: kafka-ui
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: kafka-ui
