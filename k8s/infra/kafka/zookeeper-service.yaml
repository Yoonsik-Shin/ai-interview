apiVersion: v1
kind: Service
metadata:
  name: zookeeper
  namespace: unbrdn
  labels:
    app: zookeeper
spec:
  ports:
    - name: zookeeper
      port: 2181
      targetPort: 2181
      protocol: TCP
  selector:
    app: zookeeper

